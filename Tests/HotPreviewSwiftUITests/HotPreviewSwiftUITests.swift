import XCTest
@testable import HotPreviewSwiftUI

final class HotPreviewSwiftUITests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(HotPreviewSwiftUI().text, "Hello, World!")
    }
}
